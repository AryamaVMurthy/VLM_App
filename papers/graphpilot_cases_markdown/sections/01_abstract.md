GraphPilot-Edge is a profiler-driven runtime and calibrated simulator for continuous multimodal assistant DAGs on heterogeneous mobile SoCs. The deployed runtime keeps FastVLM on the LiteRT NPU, preserves support-safe CPU execution for text and speech stages, and exposes streaming plus memory/KV control instead of hiding them behind silent fallback. The checkpoint-pinned device results report **{{WORKFLOW_A_WARM_MS}} ms** warm latency with **{{WORKFLOW_A_TTFT_MS}} ms** TTFT and **{{WORKFLOW_A_TTFS_MS}} ms** TTFS for workflow A, **{{WORKFLOW_B_WARM_MS}} ms** for workflow B, and **{{WORKFLOW_C_WARM_MS}} ms** for workflow C. The calibrated simulator is anchored to the same checkpointed evidence surface and stays within a maximum family MAE of **{{MAX_FAMILY_MAE_MS}} ms** across the pinned workload families. The paper contributes a support-safe mobile runtime, a calibrated multi-resource simulator, a broader workload universe for ranking and sensitivity analysis, and a fair baseline surface with internal baselines plus faithful method-class proxies. Every table and figure is regenerated from one canonical checkpoint manifest and one revision-bounded truth source, `{{TRUTH_SOURCE_REF}}`.
