# GraphPilot CASES Markdown Source

This directory is the human-editable source for the IEEE CASES paper.

Rules:
- edit markdown here first
- use raw `latex` fenced blocks for figures, equations, and tables that need exact IEEE placement
- do not use pipe tables; the renderer fails fast on them
- keep claims pinned to the canonical checkpoint and `Truth-docs/graphpilot_edge_revision_report.pdf`
